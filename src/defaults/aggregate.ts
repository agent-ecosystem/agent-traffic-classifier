/** Categories excluded from topPaths in daily summaries (their requests still count in category totals). */
export const DEFAULT_TOP_PATHS_SKIP_CATEGORIES = ['seo-bot', 'other-bot', 'monitoring'];

/** Default max number of top paths shown per AI bot or agent in their breakdown. */
export const DEFAULT_TOP_ITEM_PATHS_LIMIT = 10;
